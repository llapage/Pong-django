# backend
Django==4.1.13

# websocket and asynchronous task
websockets==12.0
asgiref==3.5.2

# redis cache
redis
channels==4.0.0
channels_redis==4.2.1

# production server for django (watch changement)
daphne==4.0.0
watchfiles==1.0.4
channels-postgres==0.0.5
websockets==12.0
channels_redis

# psycopg2 library to connect to PostgreSQL
psycopg2-binary==2.9.10

# file type detector
python-magic==0.4.27

# de/serialize images from model to json
djangorestframework==3.14.0

# Python image library
pillow==11.1.0
